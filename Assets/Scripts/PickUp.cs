using UnityEngine;
using System.Collections;

public class PickUp : PlayerCollidable { 
      

	// Use this for initialization
	void Start ()
    {
        collisionResult = false;
        collisionReturn = 1;
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
